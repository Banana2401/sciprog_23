#include <stdio.h>
#include <math.h>

int main() {
//  Calculate the natural logarithm of 2 and store the result in the double variable 'result'
    double result = log(2.0);
    
    // Print the result of log(2) using the %lf format specifier for double data type
    printf("log(2) = %lf\n", result);

    return 0;
}
